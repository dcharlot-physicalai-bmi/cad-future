[package]
name = "physical-proptest"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "SQLite-grade property testing — geometric invariants verified over thousands of random inputs"

[dependencies]
proptest = { workspace = true }
glam = { workspace = true }
physical-brep = { path = "../../geometry/brep" }
physical-tessellation = { path = "../../geometry/tessellation" }
physical-sketch = { path = "../../geometry/sketch" }
physical-cfl = { path = "../../core/cfl" }
physical-fea = { path = "../../simulation/fea" }
physical-units = { workspace = true }
physical-lut = { workspace = true }
