[package]
name = "physical-inverse"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Inverse CAD — mesh/scan/splat → parametric B-Rep with feature tree, constraints, and dimensions"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
glam = { workspace = true }
rayon = { workspace = true }
physical-brep = { path = "../../geometry/brep" }
physical-tessellation = { path = "../../geometry/tessellation" }
