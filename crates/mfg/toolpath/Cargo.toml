[package]
name = "physical-mfg-toolpath"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Shared toolpath types, contour operations, and G-code emitter for manufacturing"

[dependencies]
glam = { version = "0.32", features = ["serde"] }
serde = { workspace = true }
