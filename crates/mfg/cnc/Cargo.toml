[package]
name = "physical-mfg-cnc"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "CNC milling CAM — solid to G-code pipeline (2.5D and 3D)"

[dependencies]
glam = { workspace = true }
serde = { workspace = true }
physical-mfg-toolpath = { path = "../toolpath" }
physical-brep = { path = "../../geometry/brep" }
physical-tessellation = { path = "../../geometry/tessellation" }
