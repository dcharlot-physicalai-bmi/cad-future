[package]
name = "physical-emit-parasolid"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Parasolid XT transmit format reader — import SolidWorks, NX, Solid Edge geometry"

[dependencies]
glam = { workspace = true }
physical-brep = { path = "../../geometry/brep" }
serde = { workspace = true, features = ["derive"] }
